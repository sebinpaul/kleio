# Exact pins only — local / CI / server must match.
# Do not use >=. Bump a version only when you explicitly decide to.

# Django and REST Framework
Django==5.2.4
djangorestframework==3.16.0
django-cors-headers==4.7.0

# Database
mongoengine==0.29.1
pymongo==4.13.2

# Reddit API
praw==8.0.2

# HTTP requests
requests==2.34.2
aiohttp==3.12.14

# Undetected Chrome Driver for web scraping.
# 3.5.0 ignores driver_executable_path whenever it finds any patched driver in
# its data dir, and can only download pre-115 drivers. Matches the local venv.
undetected-chromedriver==3.5.5
selenium==4.34.2

# Environment variables
python-dotenv==1.1.1

# Email services
resend==2.11.0

# Authentication
clerk-backend-api==6.0.0

# Payments
dodopayments==1.111.0

# Date/time handling
python-dateutil==2.9.0.post0
langdetect==1.0.9

# Logging and utilities
colorama==0.4.6

# Production WSGI server
gunicorn==23.0.0
# Provides distutils for packages that still need it on Python 3.12+
setuptools==75.0.0
